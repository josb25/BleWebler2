name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify release
        run: |
          npm run check
          npm test

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Build web app
        run: npm run build
        env:
          VITE_BASE_PATH: /BleWebler2/

      - name: Build ULT documentation
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./packages/ult
          destination: ./packages/ult/_site

      - name: Add ULT documentation to the site
        run: |
          mkdir -p apps/web-app/dist/ult
          cp -R packages/ult/_site/. apps/web-app/dist/ult/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: apps/web-app/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
